apiVersion: v1
kind: ServiceAccount
metadata:
  # El nombre que usarás para referenciar esta ServiceAccount
  name: spm-serviceaccount2
  # Opcional: El namespace donde se creará. Si se omite, se usará 'default'.
  namespace: spm2
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: dashboard-spm2
  # Este rol existirá en el namespace del proyecto
  namespace: spm2
rules:
- apiGroups: ["", "apps", "autoscaling", "batch", "extensions", "networking.k8s.io", "storage.k8s.io"]
  resources: ["configmaps", "endpoints", "events", "ingresses", "persistentvolumeclaims", "pods", "podtemplates", "replicationcontrollers", "resourcequotas", "secrets", "serviceaccounts", "services", "deployments", "daemonsets", "replicasets", "statefulsets", "horizontalpodautoscalers", "cronjobs", "jobs", "networkpolicies", "storageclasses"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["apps"]
  resources: ["deployments/scale"]
  verbs: ["*"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: spm-dashboard-readonly-binding2
  namespace: spm2
subjects:
- kind: ServiceAccount
  name: spm-serviceaccount2 # La cuenta de servicio que creamos
  namespace: spm2
roleRef:
  kind: Role
  name: dashboard-spm2 # El rol que creamos
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: global-reader2
rules:
- apiGroups: [""]
  resources: ["namespaces"]
  verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: spm-global-readonly-binding2
subjects:
- kind: ServiceAccount
  name: spm-serviceaccount2 # La cuenta de servicio que creamos
  namespace: spm2
roleRef:
  kind: ClusterRole
  name: global-reader2 # El rol que creamos
  apiGroup: rbac.authorization.k8s.io
